# task3_webtech
Open the HTML in VS Code, identify fixed-width elements and large images, add media queries for max-width 768px to stack columns, reduce fonts, make the nav vertical, fix overflow, and ensure images scale properly.
